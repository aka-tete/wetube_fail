-> git hub website (auth)
github website (auth) -> /auth/github/callback
githubLoginCallback (profile)
    => cb(null, user)

    cookie = makeCookie(user)
    savedCookie = savedCookie(cookie)
    sendCookie(savedCookie)